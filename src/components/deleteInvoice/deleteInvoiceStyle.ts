import styled from "styled-components";
import {deleteButtonBackground, deleteButtonColor} from "../../themes/styleConst";

export const DeleteInvoiceButton = styled.button`
  color: ${deleteButtonColor};
  background-color: ${deleteButtonBackground};
  border-radius: 45px;
  padding: 0.5rem;
  width: 120px;
`
